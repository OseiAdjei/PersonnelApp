@using App.Domain
@model IEnumerable<College>

@{
    ViewData["Title"] = "Colleges";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        @if (Model != null && Model.Any())
        {
            <table class="table">
                <thead>
                    <tr class="text-center">
                        <th>@Html.DisplayNameFor(model => model.First().CollegeLogoUrl)</th>
                        <th>@Html.DisplayNameFor(model => model.First().CollegeName)</th>
                        <th>@Html.DisplayNameFor(model => model.First().CollegeProvost)</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="align-middle">
                                @if (!string.IsNullOrEmpty(item.CollegeLogoUrl))
                                {
                                    <img class="rounded-circle" src="@item.CollegeLogoUrl" alt="@item.CollegeName" style="max-width: 150px" />
                                }
                            </td>
                            <td class="align-middle">
                                @Html.DisplayFor(modelItem => item.CollegeName)
                            </td>
                            <td class="align-middle">
                                @if (!string.IsNullOrEmpty(item.CollegeProvost))
                                {
                                    @Html.DisplayFor(modelItem => item.CollegeProvost)
                                }
                            </td>

                            <td class="align-middle">
                                <a class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Edit </a>
                                <a class="btn btn-outline-info"><i class="bi bi-eye"></i>Details </a>
                                <a class="btn btn-danger text-white"><i class="bi bi-trash"></i>Trash </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No colleges available.</p>
        }
    </div>
</div>
